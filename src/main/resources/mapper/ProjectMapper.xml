<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.teamtasksystem.mapper.ProjectMapper">

    <insert id="insert" parameterType="Project" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO project(name, description, creator_id, create_time, update_time)
        VALUES (#{name}, #{description}, #{creatorId}, NOW(), NOW())
    </insert>

    <select id="selectById" resultType="Project">
        SELECT id, name, description, creator_id, create_time, update_time
        FROM project
        WHERE id = #{id}
    </select>

    <select id="selectAll" resultType="Project">
        SELECT id, name, description, creator_id, create_time, update_time
        FROM project
        ORDER BY create_time DESC
    </select>

</mapper>