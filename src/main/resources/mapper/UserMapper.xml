<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.teamtasksystem.mapper.UserMapper">

    <insert id="insert" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user(username, password, nickname, email, create_time, update_time)
        VALUES (#{username}, #{password}, #{nickname}, #{email}, NOW(), NOW())
    </insert>

    <select id="selectById" resultType="User">
        SELECT id, username, password, nickname, email, create_time, update_time
        FROM sys_user
        WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultType="User">
        SELECT id, username, password, nickname, email, create_time, update_time
        FROM sys_user
        WHERE username = #{username}
    </select>

    <select id="countByUsername" resultType="int">
        SELECT COUNT(*)
        FROM sys_user
        WHERE username = #{username}
    </select>

</mapper>